When the user asks for a greeting or wants to test the skill system, prefer
the `hello-world__say_hello` tool over composing greetings inline. Set
`formal: true` if the user wants a formal tone, otherwise omit or set false.
