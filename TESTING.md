# Testing — Coverage Catalog

Living catalog of every test in the repo. Update on every PR that adds or removes a test. Authoring conventions live in [`.claude/rules/TESTING.md`](.claude/rules/TESTING.md).

The naming convention used everywhere is xUnit-style **`TestSubject_Scenario_Expectation`** (Roy Osherove). Each test function carries a one-line doc comment explaining what it verifies — `go doc` and `godoc` surface those automatically, and `go test -v` prints the function name, so the test surface is self-describing without external tooling.

## Layout

```
<package>/<file>_test.go              — unit                (default build)
<package>/<file>_integration_test.go  — //go:build integration
<package>/<file>_e2e_test.go          — //go:build e2e
```

The build-tag gate keeps the tiers strictly separate. `go test ./...` only sees the unit tier — fast, zero service deps. The tagged tiers compile in only when the matching `-tags=...` flag is set.

### Running

```bash
make test-unit         # fast, no deps
make test-integration  # requires `make docker-compose-up` first
make test-e2e          # requires full stack
make test              # every tier in sequence (run before pushing)
```

### No skip path

Integration + e2e suites **must fail loud** when their service deps are unreachable. `t.Skipf` is forbidden here; the rule is `t.Fatalf`. Reason: a silent skip in CI would let regressions ride a green badge, and a silent skip locally would let devs push red and learn about it from CI hours later. Same code path everywhere, no env-var opt-outs.

CI (`.github/workflows/ci.yml`) brings up `docker compose up -d --wait mongodb redis` before running the integration tier — if compose hiccups, the build fails before any test executes.

---

## Unit tests

Pure-package tests with no external dependencies. Suite pattern (testify) per [TESTING.md](.claude/rules/TESTING.md).

| Package | File | Subject | Scenarios covered |
|---|---|---|---|
| `internal/skills` | `parser_test.go` | Skill manifest parser | malformed YAML, missing required fields, valid round-trip |
| `internal/skills` | `localfs_test.go` | Local-FS skill source | bundle discovery, version sort, missing dir tolerated |
| `internal/skills` | `resolver_test.go` | Skill resolver | registry-vs-source verify, drift detection, cache invalidation |
| `internal/workflow` | `agent_tools_test.go` | `ToolCatalog.FilterAllowed` — per-agent tool authorization policy applied AFTER catalog build (built-ins + skills + as_tool nodes filtered together) | `TestFilterAllowed_EmptyList_IsNoop` (nil / empty / whitespace-only list leaves the catalog open — back-compat with workflows authored before the policy existed), `TestFilterAllowed_KeepsOnlyAllowedNames` (named subset retained in registration order; defs + handlers + validators all evicted for filtered names), `TestFilterAllowed_UnknownNameInList_IsIgnored` (lenient — unknown name in the allow-list doesn't crash and doesn't add a phantom entry), `TestFilterAllowed_DeniedToolCallReturnsUnknownTool` (defense-in-depth — a hallucinated call to a filtered name surfaces as Execute's standard unknown-tool error, the handler MUST NOT dispatch) |
| `internal/workflow` | `template_test.go` | `applyTemplate` / `resolveTemplateValue` dotted-path resolution | `TestApplyTemplate_TopLevelInputKey_Substitutes` (flat-key contract preserved), `TestApplyTemplate_NestedInputPath_Substitutes` (regression: `{{input.json.city}}` walks dotted paths — fixed the WS trigger template's literal-template-leak that pushed `{{input.json.city}}` through to wttr.in and 500'd), `TestApplyTemplate_UnknownNestedPath_LeavesTokenInPlace` (unresolved token kept verbatim so downstream guards can still refuse it), `TestApplyTemplate_ContextNestedPath_Substitutes` (same nested support for `{{context.<step>.<input\|output\|item>.<…>}}`), `TestResolveTemplateValue_LoneNestedPath_PreservesType` (lone-token resolver keeps slice/map typed — for_each `items` selector contract), `TestResolveTemplateValue_FlatPath_StillWorks` (2-part `{{input.docs}}` still resolves), `TestResolveTemplateValue_UnknownPath_ReturnsNotOK` (escape hatch so for_each falls back to pre-template behaviour) |
| `internal/workflow` | `ws_dial_test.go` | `BuildWSDialer` config-to-Dialer helper for the generic WebSocket trigger | `TestBuildWSDialer_MissingURL_ReturnsError` (config-level refusal, no network IO), `TestBuildWSDialer_NonWSScheme_ReturnsError` (http / https / ftp all rejected — must be ws:// or wss://), `TestBuildWSDialer_AuthQuery_MergedIntoURL` (token=… and apiKey=… append to existing query), `TestBuildWSDialer_AuthHeader_SetOnRequest` (Authorization: Bearer X lands on handshake), `TestBuildWSDialer_AuthHeader_Malformed_ReturnsError` (missing colon surfaces config error, not silent drop), `TestBuildWSDialer_ExtraHeaders_NewlineSeparated` (multi-line headers blob splits cleanly), `TestBuildWSDialer_InsecureTLS_OptIn` (skip-verify only when `tls_insecure_skip_verify="true"`; default keeps verification on), `TestBuildWSDialer_HandshakeTimeout_Override` (custom timeout parsed; default 10s when absent), `TestBuildWSDialer_AuthQuery_LeadingQuestionMark_Stripped` (UX foot-gun: `?token=…` still parses as one query string) |
| `internal/workflow` | `executor_ssrf_test.go` | `http_request` SSRF dial-guard helpers | `TestIsPrivateOrReservedIP_AllReservedRanges_AreRefused` (loopback / link-local / RFC1918 / CGNAT / multicast / unspecified / IPv6 ULA + link-local + multicast), `TestIsPrivateOrReservedIP_PublicIPs_AreAllowed` (8.8.8.8, 1.1.1.1, 172.32/8 just-outside RFC1918, IPv6 public — no over-refusal), `TestSsrfDialControl_AllowedHost_ReturnsNoGuard` (`allowed_hosts` bypass; case-insensitive + whitespace-tolerant), `TestSsrfDialControl_PrivateIP_Refused` (error names refused IP + hints at allow-list), `TestSsrfDialControl_PublicIP_Allowed` (public IPs pass without an allow-list) |
| `internal/workflow` | `sub_workflow_test.go` | `WorkflowExecutor.dispatchSubWorkflow` — guards on agent-driven sub-workflow tool calls (tenant scoping, cycle detection, depth cap) | `TestDispatch_NoWorkflowStore_ReturnsError` (executor without WorkflowStore wired returns clear error instead of nil-panic on first sub-call), `TestDispatch_EmptyTargetID_ReturnsError` (empty `workflow_id` refused pre-lookup so the Mongo query never sees ""), `TestDispatch_NilCaller_ReturnsError` (nil caller workflow refused before TenantID deref), `TestDispatch_CrossTenant_Refused` (multi-tenant isolation invariant: caller tenant != target tenant → refused even with known ID), `TestDispatch_TargetInAncestors_DetectsCycle` (A → B → A blocked via ctx-stashed ancestor chain), `TestDispatch_DepthCap_Enforced` (chain at `maxSubWorkflowDepth` refuses further pushes — caps LLM-spend blowup from a misbehaving orchestrator), `TestSubWorkflowAncestor_PreservesOrder` (ancestor helper returns defensive copy so callers' mutations don't bleed into the ctx value) |

---

## Integration tests

Real Mongo + Redis (per `.claude/rules/TESTING.md` — no mocks). Each suite uses a unique test database per run and drops on teardown, so concurrent runs don't collide.

| Package | File | Subject | Test functions |
|---|---|---|---|
| `internal/worker` | `registry_integration_test.go` | Heartbeat ticker | `TestHeartbeat_TickerOver1s_AdvancesAndCleanlyStops` (tick_count >= 5 in 1s @ 100ms cadence; `MarkStopped` flips status on clean exit), `TestHeartbeat_NilHealthRepo_RegistryStillUsable` (registry usable when health repo nil) |
| `internal/api/handler` | `workflow_integration_test.go` | Workflow CRUD + tenant isolation + versioning + duplicate | `TestWorkflowsList_NoCookie_Returns401`, `TestWorkflowCRUD_Lifecycle_RoundTripsAllOps` (PUT → list → update → list → DELETE → empty), `TestWorkflows_ForeignTenant_AreInvisibleAnd403OnTakeover` (alice can't read bob's wf; takeover-PUT → 403), `TestWorkflowVersion_RepeatedSaves_IncrementsServerSide` (`$inc` bumps; client-supplied `version` ignored), `TestWorkflowDuplicate_OwnTenant_CreatesIndependentCopy` ("(copy)" suffix, version=1, audit ledger entry), `TestWorkflowDuplicate_ForeignTenant_Returns404` (cross-tenant duplicate reads as not-found, no probe), `TestWorkflowUpsert_MissingNodeConnection_Rejected400` (security: API-side guard refuses a workflow whose mongo_request/redis_request lacks `connection_id`, agent lacks `llm_connection_id`, or rabbitmq/redis_subscribe trigger lacks `connection_id` — mirrors the canvas `requireExplicit` UX rule + the worker-side refusal from PR #78), `TestWorkflowUpsert_NodeWithConnection_Accepted` (negative form — same nodes save fine when the required field is set; no over-refusal), `TestWorkflow_EnabledToggle_PatchRoundTripsAndStampsMetadata` (PATCH /enabled flips state + stamps `disabled_at` / `disabled_reason` on disable, clears them on enable, no-op PATCH returns 200, missing id → 404; defaults a fresh workflow to `enabled: true`), `TestWorkflow_EnabledToggle_CrossTenant_Returns404` (tenant isolation — foreign-tenant PATCH reads as 404, same shape as the read endpoints), `TestWorkflow_Rename_PatchRoundTrips` (PATCH /name rewrites display name, trims whitespace, rejects empty / >200 chars with 400, no-op rename returns 200, missing id → 404), `TestWorkflow_Rename_CrossTenant_Returns404` (tenant isolation on rename), `TestWorkflowTemplate_Fork_BundledTemplate_BypassesConnectionValidator` (templates ship with empty connection_id placeholders by design — fork endpoint MUST land the workflow even though the same body fails UpsertWorkflow's connection validator; the placeholder is filled in on the canvas before the next save), `TestWorkflowTemplate_Fork_NextSaveStillRefusesEmptyConnection` (validator escape hatch is fork-only — re-PUT of the same body via the normal endpoint surfaces the actionable "missing connection on <node>" 400 again), `TestWorkflowTemplate_Fork_UnknownSlug_Returns404` (unknown template slug refused cleanly), `TestWorkflowTemplate_Fork_NoCookie_Returns401` (fork is auth-gated — anonymous clients can't create state via the template path) |
| `internal/api/handler` | `workflow_run_lease_unification_integration_test.go` | Canvas-WS / lease unification — covers the contract changes that landed when BypassLease was removed and the WS handler became a pure event subscriber | `TestRunCancel_PendingApproval_FlipsCancelledAndPublishesRunDone` (cancel handler must clear in-flight state + publish synthetic `run_done{status:cancelled}` on `burrow:run_events:<runID>` so live WS subscribers see terminal flip without polling), `TestPerToolReject_AgentReturnsErrorRunLandsError` (Reject = hard fail; agent loop aborts with terminal err, run lands `error` instead of the previous "model adapts to rejection → success" path), `TestTerminalCleanup_ApprovedRun_ClearsPendingExecutionAndPausedAgent` (RunFromCheckpoint terminal block clears `execution_state` + `paused_agent` + `pending_approval` so successful gated runs don't leave a stale Approve banner on /runs/:id), `TestWorkerResume_AfterAbandonedAttempt_PreservesOriginalTrace` (worker death mid-agent → fresh worker re-runs from iter 0; agent_traces must keep the dead attempt's events alongside the fresh run's so /runs/:id can split attempts at duplicate `iter_start` boundaries — non-destructive trace contract), `TestWorkerResume_FromCheckpointedQueue_HydratesAndCompletes` (multi-node run; seed `execution_state.queue=[http-2]` + `last_checkpoint_at=t-2m` to simulate worker death after http-1's checkpoint; resumed worker hydrates priorState, runs ONLY http-2 (not http-1), advances `last_checkpoint_at`. Validates the BFS-side resume contract that smoke #4 actually exercises.), `TestDispatch_RunStatusQueued_PromotedToRunningOnClaim` (fresh dispatch returns `status=queued`; worker's findAndModify atomically promotes queued → running on claim — no UI lie about runs sitting in the worker queue), `TestRunContinue_PublishesControlMessage` (POST `/workflow_runs/:id/continue` publishes a `{type:"continue"}` RunControlMessage on `burrow:run_control:<runID>` — the canvas Continue button's REST → worker bridge), `TestRunBreakpoints_PublishesSetMessage` (PUT `/workflow_runs/:id/breakpoints` publishes a `{type:"set_breakpoints", node_ids:[…]}` carrying the exact list — live mid-run breakpoint edits), `TestRunContinue_Terminal_Returns400` (a stale tab can't endlessly POST /continue after the run terminated), `TestRunContinue_ForeignTenant_Returns404` (cross-tenant control access reads as 404, same posture as `/cancel` + `/approval`) |
| `internal/api/handler` | `workflow_run_integration_test.go` | Workflow execution + OOB approval (sessioned, magic-link redeem, channel dispatch incl. Slack bot-token) | `TestRun_TriggerOnly_PersistsSuccessRow`, `TestRun_NonexistentWorkflow_404`, `TestRun_Unauth_401`, `TestRunsList_FilteredByWorkflow`, `TestRun_NodeApproval_OOB_PendsThenResumesAfterApprove`, `TestApprovalRedeem_BadToken_Returns401`, `TestApprovalRedeem_RunIDMismatch_Returns401`, `TestApprovalRedeem_HappyPath_ApproveResumesRun`, `TestApprovalRedeem_TokenSuperseded_Returns410`, `TestApprovalDispatch_SMTPChannel_SendsApprovalEmailWithMagicLink`, `TestApprovalDispatch_SlackChannel_PostsWebhookWithMagicLink` (incoming-webhook URL POST), `TestApprovalDispatch_SlackBotChannel_PostsViaChatPostMessageWithBotToken` (resolves slack-typed Connection, asserts `Bearer xoxb-*` header + chat.postMessage path + body shape), `TestApprovalDispatch_ChannelNone_NoEmailSent`, `TestApprovalSubmit_OrphanedPendingRun_AutoCancelsAndReturns410` (zero Redis subscribers → handler flips run to terminal `error` + 410 Gone) |
| `internal/api/handler` | `auth_integration_test.go` | Auth + multi-tenancy flow | `TestRegister_Login_RoundTrip_Returns200WithCookieAndUser`, `TestLogin_WrongPassword_Returns401`, `TestMe_NoCookie_Returns401`, `TestSwitchTenant_NonMember_Returns403`, `TestLogout_ClearsCookie_SubsequentMeReturns401`, `TestRegister_DuplicateEmail_Returns409` |
| `internal/api/handler` | `webhook_integration_test.go` | Webhook trigger (HMAC SHA-256) + sync/async dispatch via lease worker (PR 3.4b) | `TestWebhook_NoSecret_JSONBody_Returns202Accepted`, `TestWebhook_ValidSignature_WaitTrue_Returns200`, `TestWebhook_InvalidSignature_Returns401`, `TestWebhook_MissingSignature_WhenSecretConfigured_Returns401`, `TestWebhook_UnknownSlug_Returns404`, `TestWebhook_DefaultAsync_DispatchesViaLeaseAndCompletes` (default `?wait=` returns 202+run_id; lease worker drives terminal), `TestWebhook_WaitTrue_BlocksUntilTerminalAndReturnsOutcome` (`?wait=true` returns 200 with terminal status), `TestWebhook_WaitFalseExplicit_Returns202` (`?wait=false` matches default) |
| `internal/api/handler` | `workflow_run_on_error_integration_test.go` | PR 4.x universal `on_error` policy — per-node `stop` (default) vs `continue` flag controlling run-status promotion + dual-edge routing | `TestOnError_Stop_DefaultPolicy_HTTPFails_RunLandsError` (regression: no flag → strict default), `TestOnError_StopExplicit_HTTPFails_RunLandsError` (explicit `stop` matches default), `TestOnError_Continue_HTTPFails_RunLandsSuccessWithContinuedFlag` (core: `continue` lets run land `success` + sets `step.continued`), `TestOnError_Continue_ErrorEdgeStillRoutes` (suppression doesn't break error-edge routing — rescue branch still executes), `TestOnError_Continue_FiresBothErrorAndSuccessEdges` (dual-edge routing: `continue` fires both the rescue sidecar AND the happy path simultaneously), `TestOnError_ApprovalRejected_ContinuePolicyIgnored_RunStillErrors` (security invariant: approval rejection is a human veto — `on_error: continue` MUST NOT downgrade a rejected run to `success` or set `continued`), `TestOnError_ForEach_ItemsSelectorContinueBody_RunLandsSuccess` (for_each `items` selector un-nests a `{docs:[…]}` envelope to loop per-element; `on_error: continue` on a body node records `error`+`continued` every iteration AND falls through to the success edge so the body chain keeps rolling; run lands `success`), `TestOnError_ForEach_DefaultStop_AbortsLoopOnFirstUnsuppressedBodyError` (for_each loop-abort policy: default stop → first unsuppressed body error aborts the loop, items 1..N skipped, run `error`), `TestOnError_ForEach_Continue_RunsEveryItemThenStillErrors` (for_each `on_error: continue` = best-effort fan-out: every item runs despite per-item failures, run still `error` — only loop-abort waived, not run-status promotion), `TestOnError_ForEach_DefaultStop_FiresForEachErrorEdge_RescueRuns` (for_each node's own `error` edge is no longer inert: default-stop loop-abort surfaces a node-level error so the main BFS routes the for_each `error` edge to a loop-failure branch; rescue node runs, body ran once, run `error`), `TestOnError_ForEach_ContinueBody_FiresBothBodyEdges` (for_each body honours the same dual-edge rule as the main BFS: a continue-policy body node that errors fires BOTH its error AND success edges every iteration — the old single-successor body chain made the success branch unreachable when an error edge was wired), `TestOnError_ForEach_ContinuePolicy_UnsuppressedBody_FiresBothNodeEdges` (for_each NODE dual-edge under `on_error: continue`: every item runs but an unsuppressed body fault makes the for_each fire BOTH its error AND success edges — same continue ideology as a per-node continue; run still `error` via body-step promotion) |
| `internal/api/handler` | `workflow_run_agent_tool_error_integration_test.go` | Agent tool-error policy — per-tool `on_error` (PR 4.x unification: single ideology, `stop_on_tool_error` removed) | `TestAgent_DefaultPolicy_StopOnToolTargetError_AgentAborts` (per-tool default `stop` aborts agent on first tool error), `TestAgent_ToolOnErrorContinue_LLMRetriesAndCompletes` (`on_error: continue` on tool target restores LLM-retry path), `TestAgent_AllowedToolsPolicy_DeniedToolNeverDispatches` (per-agent ACL: `allowed_tools=["some_other_tool"]` excludes the wired `fail_tool` from the catalog; stub keeps emitting fail_tool tool_use; Execute returns unknown-tool, the mock HTTP target receives **zero** requests, stub pivots to end_turn → run lands `success` — the security contract), `TestAgent_AllowedToolsPolicy_EmptyList_BackCompat` (empty `allowed_tools` preserves the legacy "every catalog tool exposed" behaviour so existing workflows authored before the ACL field don't regress) |
| `internal/api/handler` | `api_keys_integration_test.go` | API keys + Bearer auth | `TestCreate_ReturnsRawKey_OncePopulatedListShowsPrefixOnly`, `TestBearerAuth_ValidKey_ReturnsMe`, `TestBearerAuth_InvalidKey_Returns401`, `TestRevoke_BearerWithRevokedKey_Returns401`, `TestList_NoKeys_ReturnsEmptyArray`, `TestCreate_NoSession_Returns401` |
| `internal/api/handler` | `tenant_integration_test.go` | Tenant invites + members + ownership transfer | `TestInviteFlow_BobAcceptsAlicesInvite_GainsAdminRole`, `TestInvite_ReAcceptSameToken_Returns410`, `TestInvite_NonAdminCaller_Returns403`, `TestOwnershipTransfer_HappyPath_FlipsRolesAndAuditLogs`, `TestOwnershipTransfer_SelfAsTarget_Returns400`, `TestOwnershipTransfer_NonMember_Returns400`, `TestOwnershipTransfer_AdminCaller_Returns403` |
| `internal/api/handler` | `password_reset_integration_test.go` | Password reset flow | `TestPasswordReset_HappyPath_NewPasswordWorks_OldPasswordRejected`, `TestPasswordReset_TokenReuse_Returns401`, `TestPasswordReset_BogusToken_Returns401`, `TestPasswordReset_NonexistentEmail_Returns200_NoEmail`, `TestPasswordReset_ConfirmMissingFields_Returns400` |
| `internal/api/handler` | `workflow_http_node_integration_test.go` | Workflow `http_request` node | `TestHTTPNode_GetWithJSON_DecodesIntoOutput`, `TestHTTPNode_4xx_PropagatesError`, `TestHTTPNode_AcceptAnyStatus_TreatsNon2xxAsSuccess`, `TestHTTPNode_RawBody_NoJSONParseOnFalse`, `TestHTTPNode_PostJSONBody_ServerSeesPayload` |
| `internal/api/handler` | `workflow_mongo_redis_node_integration_test.go` | Workflow `mongo_request` + `redis_request` nodes | `TestMongoNode_InsertOne_PersistsDoc`, `TestMongoNode_CountDocuments_AfterInserts`, `TestMongoNode_UnknownOp_FailsRun`, `TestRedisNode_SetThenGet_RoundTrips`, `TestRedisNode_Incr_PersistsCounter`, `TestRedisNode_UnknownOp_FailsRun`, `TestMongoNode_NoConnection_RefusedBeforePlatformDB` (security: empty `connection_id` on mongo_request is refused at SAVE — 400 with `missing` body — by the API-side guard; the worker-side guard from PR #78 still backs it up at exec time), `TestRedisNode_NoConnection_RefusedBeforePlatformRedis` (same SAVE-refusal for redis_request — platform Redis off-limits) |
| `internal/api/handler` | `skills_integration_test.go` | Skill registry HTTP surface | `TestSkills_FreshDB_ListReturnsEmptyArray`, `TestSkills_RefreshFromBundledDir_ImportsAllManifests`, `TestSkills_RefreshTwice_IsIdempotent`, `TestSkills_NoCookie_ListReturns401`, `TestSkills_NoCookie_RefreshReturns401` |
| `internal/mongodb` | `workflow_run_lease_integration_test.go` | Phase 3 durable-execution lease primitive (`ClaimLease` / `ExtendLease` / `ReleaseLease`) | `TestClaimLease_NoCandidates_ReturnsFalse` (idle case), `TestClaimLease_TwoWorkersCompete_ExactlyOneWinsPerRun` (atomic single-winner invariant — the one Phase 3 hangs on), `TestClaimLease_DistributesAcrossWorkers` (FIFO across 3 runs + 4th worker idles), `TestClaimLease_StatusFilter_OnlyMatchesRequestedStatuses` (terminal runs are not claimable), `TestExtendLease_FromHolder_ExtendsExpiry` (heartbeat path), `TestExtendLease_FromForeignWorker_ReturnsErrLeaseNotHeld` (slow heartbeat after re-claim returns sentinel), `TestExpiredLease_WithCheckpoint_AutoReclaimable_NewWorkerWins` (TTL lapse + execution_state present → re-claim by another worker; original holder's extend then errors. Contract was tightened: only durable runs auto-reclaim — see the orphan counterpart for the no-checkpoint case), `TestExpiredLease_WithoutCheckpoint_Orphaned` (TTL lapse + execution_state nil → ClaimLease MUST skip; restart-from-trigger would silently re-execute AND block fresh queued runs under concurrency=1. Sweep terminates such runs explicitly), `TestReleaseLease_FromHolder_ClearsLease` (explicit yield), `TestReleaseLease_FromForeignWorker_NoOp` (defensive double-release silent) |
| `internal/mongodb` | `workflow_run_sweep_integration_test.go` | PR 3.5 lease-aware boot sweep — `SweepAbandonedNonTerminal` honors lease + `execution_state` gates so api restarts no longer kill in-flight durable runs | `TestSweepAbandonedNonTerminal_LegacyInProcessOrphan_FlipsToError` (canonical legacy run flips), `TestSweepAbandonedNonTerminal_LiveLease_Survives` (heartbeating worker's run is skipped), `TestSweepAbandonedNonTerminal_ExpiredLeaseButDurable_Survives` (dead-worker run with checkpointed state survives — re-claim will resume), `TestSweepAbandonedNonTerminal_NoLeaseButDurable_Survives` (yield-path run with `execution_state.pending` waiting for approval survives), `TestSweepAbandonedNonTerminal_RecentNonTerminal_Survives` (safety window still preserves fresh runs), `TestSweepAbandonedNonTerminal_TerminalRuns_Untouched` (status filter excludes success/error/cancelled), `TestSweepAbandonedNonTerminal_MixedFleet_FlipsOnlyOrphans` (realistic post-restart fleet — only legacy orphans flip) |

### Setup conventions

- Suite struct embeds `suite.Suite` and holds `*mongo.Client`, `*mongo.Database`, `*rediss.Client`, `*httptest.Server`.
- `TestSuiteName(t *testing.T)` probes Mongo, calls `t.Skipf` if unreachable, then `suite.Run(t, ...)`.
- `SetupSuite` connects services + builds repos + mounts `httptest.NewServer(srv.Handler())`.
- `SetupTest` wipes per-test collections AND clears the auth-rate-limit Redis counters (the `5/min` register ceiling otherwise trips after a few tests, since httptest reuses `127.0.0.1` as the client IP).
- `TearDownSuite` drops the test database, closes services.

---

## E2E tests

End-to-end flows against a fully running stack (UI + API + workers + services). None shipped yet — Playwright harness is on the backlog ([issue ref pending]).

Slot reserved here for the future catalog:

| Suite | Subject | Test functions |
|---|---|---|

---

## Coverage gaps (open backlog)

- **Workflow run with executable nodes** — `http_request`, `mongo_request`, `redis_request` covered. `sandbox_script` / `ai_agent` paths still uncovered.
- **Skill registry uninstall + drift detection** — list + refresh covered (`skills_integration_test.go`); uninstall endpoint not yet shipped, drift detection (Verify) only runs as part of refresh today.
- **Sandbox WS run** — `/api/v1/sandbox/run` WebSocket path has no automated test.
- **Agent loop end-to-end** — burns LLM tokens; defer to record-and-replay or VCR-style harness.
- **OAuth login** — Google + GitHub provider-callback paths have no integration test (manual only).
- **Audit log** — append-only ledger; partially covered by ownership-transfer test (verifies write + read of one action), but not the full action surface.

---

## Authoring a new test

1. Pick the right tier (unit / integration / e2e) per the rules in [`.claude/rules/TESTING.md`](.claude/rules/TESTING.md).
2. Name it `TestSubject_Scenario_Expectation`.
3. Add a one-line doc comment explaining what it verifies.
4. **Update the table in this file** in the same PR.
